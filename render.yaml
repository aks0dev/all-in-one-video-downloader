services:
  - type: web
    name: all-in-one-video-downloader
    env: node
    plan: free
    buildCommand: "npm install && pip install yt-dlp"
    startCommand: "npm start"
    envVars:
      - key: PORT
        value: 10000
